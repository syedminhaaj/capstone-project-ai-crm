# capstone-project-ai-crm

capstone project ai crm using react and python

To START BACKEND

Go to backend folder then

python3 -m venv venv
source venv/bin/activate

then install
pip install -r requirements.txt

Then run below command
python main.py
